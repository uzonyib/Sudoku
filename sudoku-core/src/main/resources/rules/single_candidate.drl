package sudoku;

import sudoku.core.drools.DroolsField;

rule "Apply single candidate"
salience 80
when
	$f : DroolsField(value == null, singleCandidate != null)
then
	modify($f) {
		setValue($f.getSingleCandidate());
	}
end
